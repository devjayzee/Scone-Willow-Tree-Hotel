# Architecture Audit — 2026-08-05

Audited: `src/` (180 non-test source files; 51 test files). Scripts run:
`analyze-structure.sh`, `check-circular-deps.sh`, `calculate-metrics.sh`
(hung indefinitely — replaced with manual LOC + fan-out enumeration).

Audit checks executed for all 8 rules in `.claude/rules/`. Result: **zero
hard-rule violations.** Findings below are informational (suppressed
exceptions, quantitative smells, and stale files).

## Suppressed (informational)

- `src/app/api/reports/route.ts:38` — rule-1 suppressed: "5-case type router
  where each branch is a single service-call delegation. Extracting to a
  service `getReport(type, ...)` would relocate the same switch across a
  module boundary with no consolidation... See #65 sub-item 2." Reason is
  substantive; not suspicious.

## Suspicious suppressions (informational)

None.

## Rule 1 — API routes delegate

Source: `.claude/rules/api-route-delegation.md`

No violations. All 11 route files terminate in `handleApiError`; the sole
`grep -rL` miss is `src/app/api/auth/[...nextauth]/route.ts`, which is the
documented NextAuth exception. No `@/lib/prisma` or `@prisma/client` (non-type)
imports found in `src/app/api/**`.

## Rule 2 — Services own Prisma and business logic

Source: `.claude/rules/service-layer.md`

No violations. No `next/server` or `next-auth` imports anywhere under
`src/lib/services/`. `@/lib/prisma` is only imported from
`src/lib/services/**` and `src/lib/auth.ts` (documented exception).

## Rule 3 — Zod inputs, typed wire

Source: `.claude/rules/validation-schemas.md`

No violations. No `z.object(` calls outside `src/lib/validations/`. No `:
Date` typed fields in `src/types/*.ts` (excluding `next-auth.d.ts`).

## Rule 4 — Auth at the boundary

Source: `.claude/rules/auth-guard.md`

No violations. Every handler under `src/app/api/**/route.ts` calls
`getServerSession`; the only miss is `api/auth/**`, which is the documented
NextAuth exception.

## Rule 5 — TanStack server state

Source: `.claude/rules/server-state-tanstack.md`

No violations. Zero `fetch("/api` or `fetch(\`/api` calls in
`src/components/**` or `src/app/**` (the two grep hits are `refetch()`
TanStack helpers, not the `fetch` API). No inline `queryKey: ["..."]`
literals in hooks — all keys route through `*-keys.ts` factories.

## Rule 6 — Form patterns

Source: `.claude/rules/form-patterns.md`

No violations. Zero `useForm` or `zodResolver` usages anywhere. Zero `fetch(`
calls inside `src/components/**` (excluding `refetch()`).

## Rule 7 — RSC boundary

Source: `.claude/rules/rsc-boundary.md`

No violations. Only client page/layout is `src/app/(auth)/login/page.tsx`
(documented exception). No `@/lib/services/**` imports from any `"use client"`
component.

## Rule 8 — Testing

Source: `.claude/rules/testing.md`

No violations. Zero colocated `*.test.ts` files outside `src/__tests__/`. The
three services without a same-name mirror (`booking-service.ts`,
`report-service.ts`, `staff-service.ts`) are 17–35 line re-export shims;
their real tests live under the mirrored `booking/`, `report/`, `staff/`
folders. This matches the "Known false positive" note at the bottom of
rule 8.

## Stale / unused files

### 1. Zero-import source files

- `src/components/ui/separator.tsx` — shadcn `Separator` primitive; no
  callers. Actual dividers in the app use `DropdownMenuSeparator` /
  `SelectSeparator` from their respective primitives.
- `src/components/auth/logout-button.tsx` — `LogoutButton` component; not
  imported anywhere. Logout UI lives in `src/components/layout/header.tsx`
  inline.
- `src/components/booking/booking-status-badge.tsx` — duplicated by the
  `BookingStatusBadge` defined in
  `src/components/booking/booking-table-columns.tsx:47`, which is the one
  actually imported by `booking-table.tsx`. This file is orphaned.

### 2. Test files whose mirrored source no longer exists

None. All `src/__tests__/api/**` tests target `src/app/api/**/route.ts`
handlers that exist (mapping `api/rooms-available.test.ts` →
`app/api/rooms/available/route.ts` etc.).

### 3. Exported symbols with zero external consumers (sample)

- `src/hooks/booking/index.ts:34-41` — re-exports
  `prepareOptimisticUpdate`, `rollbackBookings`,
  `useOptimisticBookingMutation`, `OptimisticContext`,
  `CreateOptimisticMutationOptions`. These symbols are used inside
  `booking-mutations.ts` via a relative import; nothing outside the
  `hooks/booking/` folder consumes the barrel re-exports.
- `src/lib/services/booking/index.ts:44-55` — re-exports
  `generateBookingRef`, `findOverlappingBooking`, `validateStatusTransition`,
  `bookingSelectFields`, `VALID_STATUS_TRANSITIONS`. Used only inside the
  `lib/services/booking/` module via relative imports; no external consumer.
- `src/hooks/booking/index.ts:45-53` — re-exports of `booking-api`
  functions (`fetchBookings`, `createBookingApi`, etc.). Only the query and
  mutation hooks in the same folder consume them, directly via relative
  imports — the barrel surface is unused externally.
- `src/hooks/use-bookings.ts` — thin shim. Only one consumer left
  (`src/hooks/use-booking-management.ts:15`); every other caller has migrated
  to `@/hooks/booking`. Similar pattern likely exists for
  `use-rooms.ts` / `use-staffs.ts`.
- `src/lib/services/booking-service.ts`, `report-service.ts`,
  `staff-service.ts` — 17–35 line re-export shims. Consumers are the API
  route handlers; safe to migrate routes to import from the modular
  `@/lib/services/<domain>` directly and delete the shims.

### 4. Files under `plans/` other than `README.md`

None. `plans/` contains only `README.md`. Clean.

### 5. TODO / FIXME / XXX / @deprecated markers in `src/`

None. The only grep hit was a doc comment referencing a booking-reference
format string (`BK-YYYYMMDD-XXX`), which is not a marker.

## Quantitative findings

- `src/components/calendar/calendar-client.tsx` — 18 imports (Ce > 12 =
  Critical per skill thresholds); 259 LOC (under warn). Fix: split calendar
  wiring (react-big-calendar setup, event handling, dialog wiring) into a
  presentational `CalendarView` and a container hook.
- `src/components/booking/bookings-client.tsx` — 12 imports (Ce > 8 =
  Warning). Fix: extract PDF-download orchestration and the toolbar into
  co-located leaf components.
- `src/components/report/reports-client.tsx` — 11 imports (Ce > 8 = Warning).
  Fix: extract the report-type switcher into its own component.
- `src/lib/services/booking/index.ts` — 9 re-exports through one barrel;
  circular-dep script flags this as elevated risk. Fix: trim the barrel to
  the surface consumers actually use (see stale-symbols above), or drop the
  barrel and have callers import from the specific submodule.
- `src/lib/services/booking/booking-mutations.ts` — 298 LOC (1 line under
  the 300 warn threshold). Fix: monitor; split off `applyBookingAction`
  routing if it grows.

No circular dependencies detected. No file exceeded the 500-LOC critical
threshold. No `:any` types. Client-component ratio: 0 pages/layouts (correct).

## Summary

- Total findings: 15 (hard=0, soft=15)
- Severity counts: Critical=1 High=2 Medium=8 Low=4
- Worst area: `src/components/calendar/calendar-client.tsx` (highest coupling)
  and stale barrel re-exports across `booking/` modules (broadest cleanup).

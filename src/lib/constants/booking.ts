/**
 * Hotel-standard check-in/check-out times, used as defaults when a guest
 * doesn't specify one. Also documented at `prisma/schema.prisma`'s
 * `checkInTime`/`checkOutTime` fields.
 */
export const DEFAULT_CHECK_IN_TIME = "14:00";
export const DEFAULT_CHECK_OUT_TIME = "10:00";
